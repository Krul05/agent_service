[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-service"
version = "0.1.0"
description = "GitHub App SDLC Agent Service: Issue -> PR -> AI review -> fix loop"
requires-python = ">=3.11"
dependencies = []
